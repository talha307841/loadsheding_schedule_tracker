{
  "name": "poweralert-pakistan-functions",
  "private": true,
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "eslint .",
    "start": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "puppeteer": "^23.11.1"
  },
  "devDependencies": {
    "eslint": "^9.17.0"
  }
}
